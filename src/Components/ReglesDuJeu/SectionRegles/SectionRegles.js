import React from "react";

const SectionRegles = ({ titre, contenu }) => {
  return (
    <div>
      <h3>{titre}</h3>
      <div>{contenu}</div>
    </div>
  );
};

export default SectionRegles;
